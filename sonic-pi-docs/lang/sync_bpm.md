# Sync and inherit BPM from other threads

## Uso

```ruby
sync_bpm  cue_id (symbol)
```

An alias for `sync` with the `bpm_sync:` opt set to true.

## Introduced in v2.10

## Example

<table>
<tr>
<th colspan="2"># Example 1</th>
</tr>
<tr>
<td><pre><code>See examples for sync</code></pre></td>
<td></td>
</tr>
</table>
