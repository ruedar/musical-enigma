# Load a built-in example

## Uso

```ruby
load_example  path (string)
```

Given a keyword representing an example, will load it into the current buffer. This will replace any previous content.

## Introduced in v2.10

## Example

<table>
<tr>
<th colspan="2"># Example 1</th>
</tr>
<tr>
<td><pre><code>load_example :rerezzed</code></pre></td>
<td># will replace content of current buffer with the rerezzed example</td>
</tr>
</table>
