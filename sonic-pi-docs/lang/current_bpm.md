# Get current tempo

## Uso

```ruby
current_bpm
```

Returns the current tempo as a bpm value.

This can be set via the fns `use_bpm`, `with_bpm`, `use_sample_bpm` and `with_sample_bpm`.

## Introduced in v2.0

## Example

<table>
<tr>
<th colspan="2"># Example 1</th>
</tr>
<tr>
<td><pre><code>puts current_bpm</code></pre></td>
<td># Print out the current bpm</td>
</tr>
</table>
