# Get current arg checking status

## Uso

```ruby
current_arg_checks
```

Returns the current arg checking setting ( `true` or `false` ).

This can be set via the fns `use_arg_checks` and `with_arg_checks`.

## Introduced in v2.0

## Example

<table>
<tr>
<th colspan="2"># Example 1</th>
</tr>
<tr>
<td><pre><code>puts current_arg_checks</code></pre></td>
<td># Print out the current arg check setting</td>
</tr>
</table>
