# Display a message in the output pane

## Uso

```ruby
puts  output (anything)
```

Displays the information you specify as a string inside the output pane. This can be a number, symbol, or a string itself. Useful for debugging. Synonym for `print`.

## Introduced in v2.0

## Examples

<table>
<tr>
<th colspan="2"># Example 1</th>
</tr>
<tr>
<td><pre><code>print "hello there"</code></pre></td>
<td>#=&gt; will print the string "hello there" to the output pane</td>
</tr>
<tr>
<th colspan="2"># Example 2</th>
</tr>
<tr>
<td><pre><code>print 5</code></pre></td>
<td>#=&gt; will print the number 5 to the output pane</td>
</tr>
<tr>
<th colspan="2"># Example 3</th>
</tr>
<tr>
<td><pre><code>print foo</code></pre></td>
<td>#=&gt; will print the contents of foo to the output pane</td>
</tr>
</table>
